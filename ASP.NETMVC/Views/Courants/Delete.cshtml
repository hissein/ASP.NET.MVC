@model ASP.NETMVC.Models.COURANTS

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Êtes-vous sûr de vouloir supprimer cela ?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.LIBELLE_COURANT)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.LIBELLE_COURANT)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.GENRES.LIBELLE_GENRE)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GENRES.LIBELLE_GENRE)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Supprimer" class="btn btn-danger btn-xs" />
            <a href="~/Courants/Index" class="btn btn-info"><span class="glyphicon glyphicon-pencil"></span> Retour a la ligne</a>
        </div>
    }
</div>
